.env
.env.prod
__pycache__/
.venv/
*.pyc
.mypy_cache/
.ruff_cache/
.pytest_cache/
data/
*.egg-info/
site/
.worktrees/
state/
eval-reports/
backups/

# Editor scratch files.
*.swp
*.swo
*~

# Local tooling state — per-developer, never shared.
# .claude/settings.json and .mcp.json ARE tracked; .mcp.json holds only
# ${VAR} placeholders. The secrets they expand to live in
# .claude/settings.local.json, which is personal and never shared.
.claude/settings.local.json
.serena/
